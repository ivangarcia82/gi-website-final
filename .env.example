# Resend API key for the contact form endpoint (/api/contact).
# Set the real value locally in .env and in the Vercel project's
# Environment Variables (Production + Preview). Do NOT commit secrets.
RESEND_API_KEY=
